#COMO LO USAMOS?
1- La primera vez ejecutamos build_vector_db.py para que se ejecute la creacion de la bdd vectorial,
si funciona veremos que se crea la carpeta chroma.
2- Como en el caso anterior donde usabamos la persistencia en un [], ponemos en funcionamiento
nuestro proyecto desde main.py

Para ejecutar servidor
 py -m langgraph_cli dev  